<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Glasgow Air Quality</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: #f5f5f5;
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .card {
      background: white;
      border-radius: 16px;
      padding: 40px;
      max-width: 500px;
      width: 100%;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
      text-align: center;
    }

    .city { font-size: 14px; color: #888; text-transform: uppercase; letter-spacing: 2px; }
    h1 { font-size: 28px; margin: 8px 0 24px; }

    .rating-badge {
      display: inline-block;
      padding: 12px 32px;
      border-radius: 50px;
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 16px;
    }

    .Good     { background: #e6f4ea; color: #2d7a3a; }
    .Moderate { background: #fff8e1; color: #f59f00; }
    .Poor     { background: #fdecea; color: #d93025; }
    .Very-Poor { background: #4a0000; color: #ffffff; }

    .advice { font-size: 16px; color: #555; margin-bottom: 32px; line-height: 1.5; }

    .readings {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin-bottom: 24px;
    }

    .reading-box {
      background: #f9f9f9;
      border-radius: 10px;
      padding: 14px;
    }

    .reading-box .label { font-size: 12px; color: #888; margin-bottom: 4px; }
    .reading-box .value { font-size: 20px; font-weight: 600; }
    .reading-box .unit  { font-size: 11px; color: #aaa; }

    .updated { font-size: 12px; color: #aaa; }
  </style>
</head>
<body>
  <div class="card">
    <p class="city">Glasgow, Scotland</p>
    <h1>Air Quality Today</h1>

    <div class="rating-badge" id="rating">Loading...</div>
    <p class="advice" id="advice"></p>

    <div class="readings">
      <div class="reading-box">
        <div class="label">NO2</div>
        <div class="value" id="no2">--</div>
        <div class="unit">µg/m³</div>
      </div>
      <div class="reading-box">
        <div class="label">PM2.5</div>
        <div class="value" id="pm25">--</div>
        <div class="unit">µg/m³</div>
      </div>
      <div class="reading-box">
        <div class="label">O3</div>
        <div class="value" id="o3">--</div>
        <div class="unit">µg/m³</div>
      </div>
      <div class="reading-box">
        <div class="label">PM10</div>
        <div class="value" id="pm10">--</div>
        <div class="unit">µg/m³</div>
      </div>
    </div>

    <p class="updated" id="updated"></p>
  </div>

  <script>
    // Fetch the latest reading from DynamoDB via API Gateway (we'll wire this up next)
    // For now we'll hardcode the latest data so the page renders correctly
    const data = {
      rating: "Moderate",
      advice: "Acceptable for most. Sensitive people (asthma, elderly) take care.",
      readings: { NO2: 9.0, "PM2.5": 5.0, O3: 87.6, PM10: 4.59 },
      fetchedAt: "2026-06-09 23:55 UTC"
    };

    // Populate the page with data
    const badge = document.getElementById("rating");
    badge.textContent = data.rating;
    badge.className = "rating-badge " + data.rating.replace(" ", "-");

    document.getElementById("advice").textContent  = data.advice;
    document.getElementById("no2").textContent     = data.readings.NO2;
    document.getElementById("pm25").textContent    = data.readings["PM2.5"];
    document.getElementById("o3").textContent      = data.readings.O3;
    document.getElementById("pm10").textContent    = data.readings.PM10;
    document.getElementById("updated").textContent = "Last updated: " + data.fetchedAt;
  </script>
</body>
</html>